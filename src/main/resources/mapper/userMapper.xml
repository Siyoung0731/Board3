<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace="com.green.menus.mapper.MenuMapper" : interface 위치 -->
<mapper namespace="com.green.user.mapper.UserMapper">
	<!-- users 목록 조회 -->
	<select id="getUserList" resultType="com.green.user.dto.UserDto">
		SELECT USERID, PWD, USERNAME, EMAIL, UPOINT, REGDATE
		FROM TUSER
	</select>
	<!-- user insert -->
	<insert id="insertMenu">
		INSERT INTO TUSER(USERID, PWD, USERNAME, EMAIL, UPOINT, REGDATE)
		 VALUES(#{userid}, #{pwd}, #{username}, #{email}, #{upoint}, #{regdate})
	</insert>
</mapper>
